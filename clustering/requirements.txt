# HoloAssist-AI — clustering / visualisation dependencies
# Compatible with Python 3.10+ (system Python on Ubuntu 22.04).
# Run from the repo root: pip install -r clustering/requirements.txt

# ── core maths ────────────────────────────────────────────────────────────────
numpy==1.26.4               # last 1.x; numpy 2.4+ requires Python 3.11+

# ── point cloud I/O ───────────────────────────────────────────────────────────
open3d==0.19.0              # read/write PCD, PLY, downsample, RANSAC plane removal

# ── clustering & dimensionality reduction ─────────────────────────────────────
scikit-learn==1.5.2         # KMeans, DBSCAN, PCA, StandardScaler

# ── visualisation ─────────────────────────────────────────────────────────────
polyscope==2.3.0            # 3D interactive point cloud viewer
